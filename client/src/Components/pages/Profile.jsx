import classes from "../../Styles/Profile.module.css";

const Profile = () => {
    return (
        <div className={`${classes["Profile"]}`}>
            <h1>Profile</h1>
        </div>
    );
};

export default Profile;
